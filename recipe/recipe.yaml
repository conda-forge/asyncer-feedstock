schema_version: 1

context:
  name: asyncer
  version: "0.0.16"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/asyncer-${{ version }}.tar.gz
  sha256: 06bbed8db624397dd79f0e7b89153fd3003a94f9cb678b6c033a1cd221108480

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pdm-backend
    - pip
  run:
    - python >=${{ python_min }}
    - anyio >=3.4.0,<5.0
    - sniffio >=1.1
    - typing_extensions >=4.12.0
tests:
  - python:
      imports:
        - asyncer
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - pip check

about:
  summary: Asyncer, async and await, focused on developer experience.
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/tiangolo/asyncer

extra:
  recipe-maintainers:
    - maresb
    - janjagusch

{% set name = "asyncer" %}
{% set version = "0.0.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/asyncer-{{ version }}.tar.gz
  sha256: 85448e0b93fb02808c7ee6b0831c06d6ed4d3ade95168ae9c241bd78d58f46ed

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.8,<4.0
    - poetry-core
    - pip
  run:
    - python >=3.8.0,<4.0.0
    - anyio >=3.4.0,<5.0
    - typing_extensions >=4.8.0,<5.0.0

test:
  imports:
    - asyncer
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/tiangolo/asyncer
  summary: Asyncer, async and await, focused on developer experience.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - maresb
    - janjagusch
